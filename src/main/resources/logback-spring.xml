<configuration>

    <appender name="CONSOLE"
              class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                %cyan(timestamp=%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX})
                %highlight(level=%level)
                %magenta(service=profile-api)
                %blue(thread=%thread)
                %red(traceId=%X{traceId:-null})
                %red(spanId=%X{spanId:-null})
                %yellow(logger=%logger)
                %green(message="%msg")
                %n
            </pattern>
        </encoder>
    </appender>
    <appender name="FILE"
              class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/profile-api.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/profile-api.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>
                timestamp=%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX}
                level=%level
                service=profile-api
                thread=%thread
                traceId=%X{traceId:-null}
                spanId=%X{spanId:-null}
                logger=%logger
                message="%msg"
                %n
            </pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
